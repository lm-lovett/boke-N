[project]
name = "boke-n-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "bcrypt",
    "pyjwt",
    "pydantic",
]

[dependency-groups]
dev = [
    "workers-py>=1.72.0",
]
